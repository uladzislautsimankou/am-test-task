Решение тестового задания на позицию Fullstack Developer (.NET + Vue).

---
## 🚀 Быстрый старт

Проект полностью контейнеризирован. Для запуска требуется только Docker.

### 1. Настройка окружения
Создайте файл `.env` в корне проекта (можно скопировать из шаблона).

*Убедитесь, что порты в `.env` не заняты другими приложениями.*

### 2. Запуск
```bash
docker-compose up -d --build
```

Приложение будет доступно по адресам:
*   **Frontend (UI):** [http://localhost:5130](http://localhost:5130) (или порты, указанные в `.env`)
*   **Backend (Swagger):** [http://localhost:5140/swagger](http://localhost:5140/swagger)

## 🛠 Технический стек

*   **Backend:** .NET (ASP.NET Core Web API, EF Core)
*   **Frontend:** Vue 3 (Composition API), TypeScript, Vite, SCSS
*   **Database:** PostgreSQL
*   **Cache:** Redis / In-Memory
*   **Infrastructure:** Docker

## 🏗 Архитектура и детали реализации

Подробное описание архитектурных решений, инструкций по локальному запуску и структуры проекта находится в соответствующих директориях:

### 🌑 [Backend README](./src/backend/README.md)
*   Логика работы сервиса и алгоритм обновления данных.
*   Схема БД, индексы и оптимизация производительности.
*   Реализация гибридного кэширования и валидации.
*   Инструкция по запуску без Docker.

### 🌕 [Frontend README](./src/frontend/README.md)
*   Структура проекта.
*   Ключевые решения.
*   Инструкция по запуску без Docker.

## 📋 Условие задания

### Задача 1. Сервис синхронизации
Построить сервис, который забирает JSON данные с [этого адреса](https://raw.githubusercontent.com/biggiko/nasa-dataset/refs/heads/main/y77d-th95.json) и сохраняет их в базу данных.

**Требования:**
*   Сервис должен запускаться периодически по расписанию.
*   Нужно предполагать, что возвращаемые JSON данные могут меняться (добавляться, удаляться, изменяться).
*   Нужно обновлять данные в локальной базе с учетом этих изменений.

**Что интересно в вашем решении:**
*   Как вы построите архитектуру сервиса
*   Как будете забирать данные из стороннего сервиса (оптимальный метод, обработка ошибок и т.д.)
*   Как вы построите схему базы данных (типы данных, индексы и т.д.)
*   Как вы будете сохранять данные в базу (оптимальный метод)
*   Стиль кода и следование гайдлайнам C#

### Задача 2. API Метод
Создать API метод, который будет возвращать отфильтрованные, сгруппированные и отсортированные данные из шага 1.

**Функционал:**
*   **Фильтры:** По диапазону лет (напр. 1970-1980), по классу (`recclass`), по части названия метеорита (вхождение).
*   **Группировка:** Данные группируются по *году*.
*   **Сортировка:** По году, по количеству, по суммарной массе.

**Что интересно в вашем решении:**
*   Валидация входящих параметров
*   Выборка данных из базы (оптимальный метод)
*   Кэширование
*   Обработка ошибок

### Задача 3. Frontend (UI)
Создать страницу, которая выведет данные из API в табличном виде.

**Функционал:**
*   **Фильтры:** Год с, по (выпадающие списки), класс метеорита (выпадающий список), часть названия метеорита (текстовое поле поиска)
*   **Сортировка:** Всех колонок по возрастанию и убыванию.
*   **Пример таблицы:**

| Год | Количество метеоритов | Суммарная масса |
| :--- | :--- | :--- |
| 1980 | 12 | 100 |
| 1981 | 4 | 200 |
| **Total** | **16** | **300** |

**Что интересно в вашем решении:**
*   Как вы построите архитектуру страницы
*   Способ обращения к API, обработка ошибок
*   Стиль кода и следование гайдлайнам HTML, CSS, JS/TS
